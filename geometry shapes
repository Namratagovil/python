import turtle

# Set the number of sides here.
sides = 3

# Set the length of a side here.
length = 100

t = turtle.Turtle()
t.color("orange")
for side in range(sides):
    t.forward(length)
    t.right(360/sides)
